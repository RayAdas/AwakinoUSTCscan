我正在研究超声波C-Scan检测项目，并应用一些神经网络技术做缺陷检测和三维重建。

超声波C-Scan技术的扫描的原始数据可以视作一个三维数组，其中每个元素的(x,y,z)，x,y代表C扫平面的坐标(以下称为坐标点),z代表该坐标点下采样点，而元素的值则代表该时间的回波信号强度。例如[1e-3,1e-3,1e-6]代表了位于C平面的1mm 1mm坐标处的点，在超声激励信号发射1微秒后的回波信号强度。

这样的三维原始数据和图片的原始数据具有显著的区别：图片数据只有两个维度是连续的（即在空间上连续）而第三个维度（无论是RGB,  HSV或者其它高维特征）中的各个层并没有连续性；而超声数据的第三个维度代表飞行时间或者深度，也具有连续性。

我们只关心第一个显著的回波界面。即不考虑目标物体中某个点下同时存在裂缝和底部边界，造成两次回波的情况。据此，存在一个真实数据集$depth_{x,y} = R(x,y)$。

传统的最简单的C-Scan方案会先计算每个点的ToF，将数据降维到2维然后针对这个二维数据处理。如果超声探头没有直径，可以精确地测量一个坐标点下的回波，那么确实只需如此就可以测量出R。

但实际超声波探头具有直径，每个坐标点处所采集到的信号并不只取决于该点的深度，超声探头的直径会导致整个直径区域内的待测物体形状均会对该点数据造成影响。例如当超声探头处在两个不同深度的区域分界上时，探头一半在材料较薄处，一半在材料较厚处，返回单一的ToF只能查看到一个介于二者之间的值。这会导致产生的R不够锐利，无法准确反映原来的厚度情况。

这正是本项目要解决的问题，即探头直径导致的重建结果不够锐利。这是因为传统方案在将数据降维到2维时，只保留了一个ToF信息，丢失了有价值的信息。而通过直接观察该点处的回波波形，可以分析出薄区和厚区两个回波，这或许对于缺陷分析和三维重建有所帮助。

举例说明：
探头沿着x方向，从厚度5区域到厚度10区域移动时，其V(t)波形将会由一个$\tau = 5/声速$的单峰波形，过渡到双峰波形，再过渡到一个$\tau = 10/声速$的单峰波形。

单个边界的超声回波建模如下，其具有7个待拟合参数，我猜测$\tau$与$\beta$对后续的缺陷检测与三维重建的重要性更高，但其它参数仍可以辅助训练：
$$\text{env}(t) = \beta \exp \left(-\alpha \left(1 - r \tanh \left(m \left(t - \tau\right)\right)\right) \left(t - \tau\right)^2\right)$$

$$s(t) = \text{env}(t) \cos \left(2\pi f_c \left(t - \tau\right) + \psi \left(t - \tau\right)^2 + \phi\right)$$


一些我目前研究过的，可能有用的技术路线：
- 将信号处理数据，如包络（对仿真数据为env，对真实数据使用希尔伯特变换）一同输入
- 对于单个波形的特征提取或压缩，（因为我觉得直接输入原始的三维数组，输出底板形状的这种应该很难训练得出来）。先对z维度进行压缩，但会引出回波数量不定的问题，为了解决这一问题，又有
  - 输出heatmap，但是heatmap和输入等长，相当于没压缩
  - 使用transformer输出不定长参数，优势在于直接使用7个物理参数对这一部分的网络做训练，但是输入后续的缺陷检测或者重建时又需要再次编码
- 使用神经网络做tau的初始参数估计（精确到半波长级别），然后根据回波公式，将其转换为一个最优化问题。在半波长范围内，优化不再是一个非凸优化问题，可以简单地解决。

